# Fable 5 提示词实战指南

> 整理自中英文社区十几篇实战帖。核心结论：Fable 5 最值得做的事只有两类——**① 让它自己跑实验**（你睡觉它干活），**② 让它研究你**（把你的工作方式变成可复用系统）。其他用法都是这两类的子集。

---

## 一、核心方法论：目标 · 指标 · 边界

Karpathy 的总结是最佳的 /goal 提示语结构：

> "Here's an objective, here's a metric, here's your boundaries. And go."

写 prompt 只需说清三件事，然后放手：

| 要素 | 说明 |
|------|------|
| 目标 | 要什么结果 |
| 指标 | 怎么判断做完了 |
| 边界 | 什么东西不能碰 |

---

## 二、让它自己跑实验（/goal 自主实验）

**案例**：Superpowers 作者 Jesse Vincent 睡前给 Fable 5 一个 /goal，醒来时 25 个实验跑完，花 $165 换来构建速度 +50%、token 开销 -60%。

**关键点**：要求记录每个实验的假设和失败路线。Fable 5 中途自己发现并修正了 3 个测量 bug——无人介入的自我纠错。

```
/goal 任务完成后，再专门跑一轮成本优化研究，让 Opus 负责统筹，
持续记录"我为什么这么试、预期会怎样、实际结果如何"。
至少做 25 次不同实验，每次实验开始前先写清楚假设，失败路线也要完整记录。
如果中途发现数据量错了、口径不一致，或者评估方法有问题，要当场修正并继续。
```

### 已验证无效的"省钱"方案（不要踩坑）

| 直觉方案 | 实测结果 |
|----------|----------|
| 给思考过程加字数上限 | 适得其反：92 轮的任务变 138 轮，输出翻倍更贵 |
| 便宜模型（Sonnet）写计划 + Fable 5 执行 | 计划看着没问题，但不是 Fable 的思路，任务结构全乱 |
| 给测试计划设字数预算 | 直接削减 62% 测试内容——等于让研究员删掉一半实验 |

---

## 三、让它研究你（工作模式六问）

最被低估的用法：让 Fable 5 分析你的历史使用模式，打包成 Skills / 指南 / 系统指令，交给 Opus 4.8 或其他模型执行。切换模型后，它已经知道你是谁。

```
遍历我 Claude 所有的历史聊天记录、重复提示、文档、项目、Skills 和工作流程。然后回答：
1. 我最常使用 Claude 做什么？
2. 哪些任务我总是重复地做？
3. 哪些指令我总是手动重写？
4. 哪些工作流程应该变成可重用的 Skills？
5. 过去做了哪些事的方法和思路是错的，以你的视角应该要避免？
6. Opus 应该知道什么，这样它就能给我 90% 的 Fable 5 体验？
把所有这些转化为 Claude Skills、使用指南、工作流程模板，以及给 Opus 的系统指令。
```

---

## 四、行为规范（贴在每个 subagent 提示语末尾）

开发者 hiromi maeo 从 Anthropic 的 Fable 5 文档反向提炼。用途：subagent 里的模型（Opus/Sonnet）不如 Fable 5 聪明，不明确规范就会磨蹭、重复劳动、说"这就去做"然后停住。贴上这份规范，subagent 输出更易读，也支持"Fable 5 出计划、其他模型执行"的 API 打法。

```
结论先行｜每次汇报第一句话就说「发生了什么」或「发现了什么」，不要让我翻三页才看到结果。
立即行动｜信息够了就动手。不要重新推导已经确定的事实，不要重新讨论已经关闭的决定，不要列一堆你不会采用的方案。
实证汇报｜报告前用工具结果交叉验证。没验证的说没验证，测试失败的把输出原样贴出来。编造进度是最恶劣的失败。
最小范围｜不做要求以外的功能、重构或抽象化。做最小能用的东西。不要写防御不可能发生的场景的代码。
说到做到｜永远不要说「这就去做」然后停下来。执行完再终止。
```

---

## 五、Subagent 任务分配（降 token 消耗）

让 Fable 5 自己判断任务下放：小任务给便宜模型，重任务自己跑。更极端的玩法：所有执行走 codex subagent，Fable 5 只负责计划和头脑风暴。API 计费后多 Agent 分配大概率成主流。

```
对于任何编码任务，自行判断选择低算力模型并在 subagent 中运行。
小任务交给便宜模型，重任务保留全力运行。
```

---

## 六、Loop 自动化工作流

开发者 Machina 把"让 AI 自己判断何时做什么"固化成 25 个可直接抄的 loop（Claude Code 上按固定时间间隔运行）。

**每个 loop 的五要素**：排期表（何时醒来）、每轮只改一件事、统一检查标准（本周分数和上周可比）、状态文件（做了什么、下一步是什么）、停止条件（轮次上限 + 完成/堵住的定义）。

**颜色分级**：🟢 绿色完全自动跑；🟡 黄色需审批才能发出；🔴 红色涉及生产环境或外发消息，不能无人自动跑。

**核心机制**：状态文件。模型每轮先读上一轮历史，永不重做已完成的工作，每次运行比上一次更聪明。

### 最值得抄的模板：Shadow Prompt Loop（提示语 A/B Test）

新旧 prompt 同时跑真实流量，用数据决定好坏：

```
/loop daily, on real traffic
读取 shadow-prompt-STATE.md，里面记录了：
- baseline prompt（当前线上版本）
- candidate prompt（待验证的新版本）
- 已积累的 disagreement cases（两个 prompt 输出不一致的真实请求）

本轮任务：
1. 从今天真实流量中抽取 N 条代表性请求（边缘 case、高频场景各占一半）
2. 同时用 baseline 和 candidate 各跑一遍，记录输出
3. 只记录有分歧的 case（输出明显不同），相同的跳过
4. 每个分歧 case 标注：哪个更好、为什么、是否 candidate 一致性更优

约束：
- 不直接替换线上 prompt，只积累对比数据
- 积累 50 个有效分歧 case 后，生成一份「是否切换」的推荐报告
- 报告必须包含：candidate 胜/平/负的比例，典型分歧场景，切换风险评估

State file: shadow-prompt-STATE.md（记录：已测多少条、disagreement 堆、当前结论）
Stop: 50 个分歧 case 或 14 天
Color: 🟢 Green（只读流量 + 写 state file，不改线上配置）
```

---

## 七、Anthropic 官方模板（可设成 hook）

### 7.1 自治运行 + 自动暂停

长时间运行时只在真正需要人介入的关键点停下来：

```
只在工作真正需要我介入时暂停：破坏性或不可逆的操作、真正的范围变更、
或者只有我才能提供的信息。其他情况继续推进，完成后汇报。
```

### 7.2 记忆更新（错题本）

可设成 hook，说「记一下」自动触发。换模型/换账号后保留经验：

```
每条经验单独存成一条记录，开头用一句话概括。
只记录两类东西：
1. 被我纠正过、下次不能再犯的点
2. 已经验证有效、以后可以复用的方法
每条记录要写清楚它为什么重要。
不要记录仓库、文档或聊天记录里已经有的信息。
如果已有相似记录，就更新旧记录，不要重复新建。
如果后来发现某条记忆是错的，就删掉或标记废弃。
```

---

## 八、动手前的两步流程

### 8.1 反向面试（写需求文档时）

```
问我问题，直到你有 95% 的把握可以把这件任务完成。
```

### 8.2 /goal 结构化模板（问清后开 plan 模式做初版）

```
背景：我在为 [谁] 做 [什么项目]，他们需要 [这个东西能帮他们解决什么问题]。
具体要求：[用一句话说清楚你要什么]
交付格式：[你希望最终结果是什么样的，怎么给你]
红线：[过程中绝对不能做什么事]
```

---

## 九、一条工作原则

Claude Code 团队工程总监 Fiona Fung：遇到重复性工作就条件反射地问"能不能自动化"——**一件事重复三遍以上，就想尽一切办法让 AI 自动化掉**。

不要担心烧 API 额度：最好的模型本身就是最便宜的实现路径。模型会换、窗口会关，但沉淀下来的工作模式、错题本和对话记忆不会消失。

---

## 附：Prompt 速查索引

| # | 名称 | 用途 |
|---|------|------|
| 1 | /goal 自主实验 | 睡前布置，让模型通宵跑实验并记录假设 |
| 2 | 工作模式六问 | 让模型研究你，产出 Skills 和系统指令 |
| 3 | 行为规范 | 贴在 subagent 提示语末尾，规范执行行为 |
| 4 | Subagent 分配 | 让模型自己决定任务下放，降低 token 消耗 |
| 5 | Shadow Prompt Loop | 新旧 prompt 走真实流量 A/B Test |
| 6 | 自治运行 + 自动暂停 | 长时间运行，只在关键时刻停下问人 |
| 7 | 记忆更新 | 错题本 + 有效方法沉淀，跨会话保留经验 |
| 8 | 反向面试 | 执行前让模型追问到 95% 有把握 |
| 9 | /goal 结构化模板 | 背景/要求/交付格式/红线 四段式 |
